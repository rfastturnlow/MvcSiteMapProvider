@model MvcMusicStore.ViewModels.StoreIndexViewModel
@{
    ViewBag.Title = "Store";
}
<h3>Browse Genres</h3>

<p>Select from @Model.NumberOfGenres genres:</p>
<ul>
    @foreach (var genre in Model.Genres)
    {
        <li>@Html.ActionLink(genre, "Browse", new { genre = genre })</li>
    }
</ul>
